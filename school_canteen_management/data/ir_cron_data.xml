<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_low_balance_check" model="ir.cron">
        <field name="name">Canteen: Daily Low Wallet Balance Check</field>
        <field name="model_id" ref="model_student_wallet"/>
        <field name="state">code</field>
        <field name="code">
all_wallets = model.search([])
low_wallets = all_wallets.filtered(lambda w: w.balance &lt;= w.low_balance_threshold)
for w in low_wallets:
    w._send_low_balance_alert()
        </field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>
</odoo>
